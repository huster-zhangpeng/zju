#include <iostream>
#include <stdio.h>

using namespace std;

int N;
int piece[13];

int main(){
    int i, j, sum, min;
    while(cin>>N && N){
        sum = 0;
        for(i = 0; i < N; i++){
            cin>>piece[i];
            sum += piece[i];
        }
        sum
    }

    return 0;
}
